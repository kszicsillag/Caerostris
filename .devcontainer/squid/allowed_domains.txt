# Grown from evidence (docker compose logs proxy), not guesswork - see
# .claude/sandbox-design.md. A denied request names the missing domain;
# add it here and `docker compose restart proxy`.

# Anthropic / Claude Code (api.anthropic.com, claude.ai sign-in,
# platform.claude.com auth, mcp-proxy.anthropic.com, code.claude.com docs
# lookups by claude-code-guide)
.anthropic.com
.claude.ai
.claude.com

# Package registries
.nuget.org
.npmjs.org
.npmjs.com

# apt (playwright's `install-deps` step installs Chromium's OS-level shared
# libraries via apt-get)
.ubuntu.com

# Microsoft.Playwright NuGet package's browser binary download (the
# run-caerostris skill's `driver.cs -- install` step). cdn.playwright.dev
# redirects to Google's Chrome for Testing bucket for the actual zip.
cdn.playwright.dev
storage.googleapis.com

# C# Dev Kit (ms-dotnettools.csharp): optional components fetched on
# extension activation - Copilot Roslyn language server integration and the
# WASM debugger bridge. Not blocking (extension degrades gracefully without
# them - see "could not be found" warning), but avoids the 403 noise.
roslyn.blob.core.windows.net
vsdebugger.blob.core.windows.net

# GitHub (gh CLI, git over HTTPS, raw content served to MCP/WebFetch)
.github.com
.githubusercontent.com

# microsoft-learn MCP server (.mcp.json)
learn.microsoft.com

# VS Code Server + Marketplace bootstrap. ~/.vscode-server is persisted in
# a named volume (docker-compose.yml), so these are only hit on first
# creation of that volume or after a VS Code Server version bump, not
# every rebuild.
vscode.download.prss.microsoft.com
update.code.visualstudio.com
marketplace.visualstudio.com
download.visualstudio.microsoft.com
.vsassets.io
.vscode-cdn.net
