@namespace Caerostris.Shared.Info


    <PageTitle>@(PlaybackContext.ValidTrackItemOrNull() is FullTrack track ? $"{CaerostrisTitle} — {track.Name}" : CaerostrisTitle)</PageTitle>


@code 
{
    [CascadingParameter]
    public CurrentlyPlayingContext? PlaybackContext { get; set; }

    private const string CaerostrisTitle = "Cærostris";
}
